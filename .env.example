OPENAI_API_KEY=sk-your-key-here
PORT=3000

# Optional — OpenAI text-to-speech for playback (clearer than phone browser voices)
# OPENAI_TTS_MODEL=tts-1-hd
# OPENAI_TTS_VOICE=nova
# Optional. Default 0.0.0.0 (listen on all interfaces — use on Railway/Docker). For local-only: HOST=127.0.0.1
# HOST=127.0.0.1
